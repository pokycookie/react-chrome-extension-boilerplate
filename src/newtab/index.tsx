import * as React from 'react'
import * as ReactDOM from 'react-dom'

import App from './App'
import './index.css'

var mountNode = document.getElementById('newtab')
ReactDOM.render(<App />, mountNode)
